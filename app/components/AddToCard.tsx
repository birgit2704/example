"use client";
import React from "react";

const AddToCard = () => {
  return (
    <button className="btn btn-accent" onClick={() => console.log("clickk")}>
      Add to cart
    </button>
  );
};

export default AddToCard;
